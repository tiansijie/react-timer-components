{
  "name": "react-timer-components",
  "version": "1.0.0",
  "description": "",
  "main": "dist/commonjs/index.js",
  "module": "dist/es/index.js",
  "scripts": {
    "build5": "babel src --out-dir dist/commonjs --presets=env,react",
    "build6": "babel src --out-dir dist/es",
    "build": "npm run build5 && npm run build6",
    "prepublish": "npm run build"
  },
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-env": "^1.6.1",
    "babel-preset-react": "^6.24.1"
  },
  "peerDependencies": {
    "react": "^16.0.0"
  },
  "dependencies": {
    "lodash.debounce": "^4.0.8"
  }
}
